# The Google API key in src/api.zig is the public InnerTube (YouTube Music
# WEB_REMIX) client key shipped in every youtube music web page. It is not a
# secret and is required to reach the public youtubei endpoints, so it is
# excluded from secret scanning to avoid false-positive alerts.
paths-ignore:
  - "src/api.zig"
